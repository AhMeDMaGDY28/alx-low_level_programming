#include <stdio.h>
/**
 * main - Entery point to the program
 * @argc: the number of arguments and it is not used
 * @argv: the argument written in running
 * Return: always zero
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
