#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * This function computes the absolute value of an integer 'a'.
 *
 * @a: The integer to compute the absolute value for
 *
 * Return: The absolute value of 'a'.
 * Code made by Ahmed Magdy.
 * ALX CO 1 blended
 */
int _abs(int a)
{
	int b = -1;
	int c = a * b;

	if (a < 0)
	{
		_putchar('c');
	}
	else
	{
		_putchar('a');
	}
	return (c);
}

